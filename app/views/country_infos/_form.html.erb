<%= form_for(@country_info) do |f| %>
  <% if @country_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@country_info.errors.count, "error") %> prohibited this country_info from being saved:</h2>

      <ul>
      <% @country_info.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :country_id %><br />
    <%= f.number_field :country_id %>
  </div>
  <div class="field">
    <%= f.label :country_name %><br />
    <%= f.text_field :country_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
